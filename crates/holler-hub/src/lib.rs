//! `holler_hub` — the hub role of the single `holler` binary (ADR 0001).
//!
//! Empty in the workspace skeleton (story #327); the protocol/hub stories
//! fill it in. Dependencies are declared (inherited from the workspace,
//! pinned in ADR 0003) so the crate compiles from the start.
