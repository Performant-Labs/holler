//! `holler_proto` — the wire protocol types.
//!
//! Empty in the workspace skeleton (story #127); the protocol port fills it
//! in under ADR 0004 (wire protocol v2). The two serde dependencies are
//! declared here (and inherited from the workspace) so the crate compiles
//! from the start.
