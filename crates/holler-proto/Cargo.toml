[package]
name = "holler-proto"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Wire protocol types for holler (empty in the workspace skeleton)"

[lib]
name = "holler_proto"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
