# Holler protocol v2

Holler is a **talk circuit**. This page is the **v2** application
protocol: **JSON-RPC 2.0** messages on a **WebSocket** between the
**hub** and the **body**. This file is **canonical**: ADRs decide why,
stories implement this page. It supersedes [v1](v1.md) (v1 is not
ported; see [ADR 0002](../adr/ADR-0002.md)).

| | |
| --- | --- |
| Index | [All protocols](README.md) |
| ADR | [0004](../adr/ADR-0004.md) (JSON-RPC 2.0, envelope) · [0005](../adr/ADR-0005.md) (names) · [0006](../adr/ADR-0006.md) (transport) |
| Implement | holler-server [#331](https://github.com/Performant-Labs/holler-server/issues/331) → [holler #136](https://github.com/Performant-Labs/holler/issues/136) |
| Body | Speaks **ACP v2** behind this wire ([ADR 0004](../adr/ADR-0004.md)) |

## 0. Guiding documents

The v2 object model and envelope are **reused verbatim** from three
published specs; Holler adds only the thin circuit on top. Pinned
revisions:

| Document | Revision | URL | Role in v2 |
| --- | --- | --- | --- |
| A2A | **v1.0.1** (tag `v1.0.1`, published **2026-05-28**; 1.0.0 was 2026-03-12) | <https://a2a-protocol.org/latest/specification/> | The **object model**: `Message`, `Part`, and the task-state vocabulary. Reused **verbatim** on the inward wire. |
| ACP | **v2** (crate `agent-client-protocol` 2.1.0) | <https://github.com/agentclientprotocol/agent-client-protocol> | The **body hop**: the body's driver maps `session/prompt`/`session/update`/`session/cancel` 1:1 to ACP. ACP's `stopReason` is the source of truth. |
| JSON-RPC | **2.0** | <https://www.jsonrpc.org/specification> | The **envelope**: requests, notifications, responses, error objects, and the standard error codes. |

## 1. Transport

- **WebSocket text frames.** Exactly **one JSON-RPC 2.0 message per frame**.
- **No batches.** A JSON **array** at the top level (a JSON-RPC batch) is **rejected with `-32600`** and the socket is closed. Batching is deferred to a future protocol version.
- **Binary frames are not a message.** A binary frame is rejected with `-32600` (see §8).
- **Loopback is plain `ws`; off-loopback is `wss`.** Plain `ws` is **loopback only** (`127.0.0.1` / `::1`). Off-loopback (LAN, tailnet, public) is **`wss`** via a TLS proxy ([ADR 0006](../adr/ADR-0006.md)). The listen address is user-settable (repeatable); default **`127.0.0.1:41807`**, IPv6 `[::1]:41807`. Do not default to `localhost`.
- Any underlay (LAN, SSH tunnel, Tailscale, public IP) may create the TCP path. It is not Holler auth, not Holler addressing, and **not Holler encryption**.

## 2. Envelope

JSON-RPC 2.0. Four wire shapes (see `holler_proto::envelope`):

| Shape | Fields | Notes |
| --- | --- | --- |
| Request | `jsonrpc`, `id`, `method`, `params`? | `id` present |
| Notification | `jsonrpc`, `method`, `params`? | **no `id`** |
| Response | `jsonrpc`, `id`, `result` | `id` echoes the request |
| Error | `jsonrpc`, `id`, `error` | `error: {code, message, data?}` |

**Correlation ids are string ULIDs with a minting-prefix:**

- `h-` — minted by the **hub**
- `b-` — minted by the **body**

The prefixes guarantee the two sides' id spaces **never collide** (a
`b-` id can never equal an `h-` id). A **reply reuses the request's
`id`** — that is what correlates a response to its request (this replaces
v1's `id` reuse on the envelope). **Notifications carry no `id`.**

`id` is **stringly-typed on the wire**; the prefix is a minting
convention, not a wire field a peer must validate. A response whose
`id` does not match any outstanding request is an **unmatched response**
(see §8 / §11).

## 3. Connection

```
connect → circuit/join → (hub closes)      ← first pairing only
connect → circuit/authenticate → circuit/hello (both ways) → talk
```

1. **`circuit/join`** — legal **only** as the **first frame on a fresh socket**, and **only** on a socket that has not sent `circuit/authenticate` (a socket sends join or authenticate, never both). It redeems a one-time **join secret** for a `client_id` + long-lived `credential`. On success the hub **closes the socket after replying** — join never leads into talk on the same socket; it is a one-shot bootstrap. A new join token is only for a new pairing.
2. **`circuit/authenticate`** — the **first frame** on a normal (re)connect, presenting the **credential** (not the join secret). Do not put the credential in a logged URL query string.
3. **`circuit/hello`** — sent **both ways** after authenticate. Carries `protocol` / `protocol_min` / `protocol_max` plus the role-appropriate capabilities document (§5). **`hello.protocol ≠ 2` → error `-32000 unsupported_version` and the socket closes. No silent downgrade.**
4. After **both** hellos, `session/…`, `query/…`, and `circuit/ping` are legal.

**Reconnect** re-authenticates with the credential again and re-sends a
complete, fresh `circuit/hello` **and** `session/presence` on every
successful (re)connect — the peer treats each advertisement as
authoritative, never a merge with prior state (the v1 reconnect
contract, carried over).

## 4. Method catalog

| Method | Kind | Direction | params | result |
| --- | --- | --- | --- | --- |
| `circuit/join` | request | body→hub, first frame on a fresh socket | `{secret, hostname}` | `{client_id, credential}`; hub **closes the socket** after replying |
| `circuit/authenticate` | request | body→hub, first frame | `{token_id, credential, hostname}` | `{ok: true}` |
| `circuit/hello` | request | both, after auth | the hello document (§5) | `{}` |
| `circuit/ping` | request | both | `{}` | `{hostname, ts}` |
| `circuit/superseded` | notification | hub→old socket | `{}` | — (then the hub **closes** it) |
| `query/status` | request | both | `{}` | the status document (§5.1) |
| `query/caps` | request | both | `{}` | the caps document (§5.2) |
| `query/support` | request | both | `{feature}` | the support answer (§5.3) |
| `query/protocol` | request | both | `{version?}` | `{session:2, min, max, asked?, ok?}` |
| `session/presence` | notification | body→hub | `{hostname, sessions:[{name, harness, state, mode, harness_session_id?}]}` — `state` names are A2A's | — |
| `session/prompt` | request | hub→body | `{session, message, meta?}` (`message` is an A2A `Message`, `role:"user"`) | `{stopReason, state, message}` (§6) |
| `session/update` | notification | body→hub | `{session, prompt_id, seq, parts:[Part]}` (a streamed text chunk is a text `Part`; the coalescer merges consecutive text parts) | — |
| `session/cancel` | request | hub→body | `{session}` | `{applied: true}` — sent **only after** the cancel was applied |
| `session/answer` | request | hub→body | `{session, choice}` | `{applied: true}` — sent **only after** the driver resumed the turn (issue #151) |

`session/cancel` is **control, not a prompt**: it cancels the in-flight
**turn**; the session survives and stays promptable. It never routes into
a model.

`session/answer` resolves one held `input-required` permission/elicitation
(§10.1's `answer SESSION CHOICE` CLI verb): `choice` is a 0-based index, an
exact (case-insensitive) label/key match, or — for a multi-field
elicitation — a comma-separated list, one segment per field, in the same
order as that field's own `PendingItem.options` (§7). A session with
nothing pending answers `-32010 nothing_pending` (§8) instead of a result;
a `choice` that does not resolve, or an elicitation shape the driver does
not resolve (a `url`-mode elicitation, or a freeform non-enum field),
answers `-32602 invalid_params`. It is dispatched exactly like
`session/cancel` — `methods.rs::CATALOG` has it (`HubToBody`, `Request`),
and the body's own dispatch (`connection.rs`/`prompt_dispatch.rs`) and the
session-manager (`SessionManager::answer`, `holler#342`) are real, not a
stub.

### Hello document

A **body's** hello:

```json
{
  "protocol": 2, "protocol_min": 2, "protocol_max": 2,
  "role": "body", "hostname": "kiwi",
  "token_id": "tok_7f3a", "client_id": "cli_19",
  "features": ["interrupt", "presence", "ping", "query"],
  "harnesses": ["opencode"],
  "sessions": [
    {"name": "alpha", "harness": "opencode", "mode": "spawn"},
    {"name": "beta", "harness": "opencode", "mode": "attach"}
  ]
}
```

A **hub's** hello (the hub is a **peer** for status — same document
shape, `role:"hub"`, and it has `harnesses_known` /
`harnesses_confirmed` instead of `harnesses` / `sessions`):

```json
{
  "protocol": 2, "protocol_min": 2, "protocol_max": 2,
  "role": "hub", "hostname": "uranus",
  "features": ["interrupt", "presence", "ping", "query", "roster", "token", "attach", "opencode-http"],
  "harnesses_known": ["opencode"],
  "harnesses_confirmed": ["opencode"]
}
```

**Advertise only what is real.** A body lists a harness only if it can
spawn/attach that adapter on this box. `hello` is a hint;
`query/support` is the check.

## 5. Documents

### 5.1 Status document

One shape, two `role` values (the hub as a peer answers the same
document). **Body:**

```json
{
  "role": "body", "protocol": 2, "protocol_min": 2, "protocol_max": 2,
  "hostname": "kiwi", "connected": true, "token_id": "tok_7f3a",
  "features": ["interrupt", "presence", "ping", "query"],
  "harnesses": ["opencode"],
  "sessions": [
    {"name": "alpha", "harness": "opencode", "state": "idle"},
    {"name": "beta", "harness": "opencode", "state": "idle", "endpoint": "http://127.0.0.1:4096"}
  ]
}
```

`endpoint` (issue #195) is present only for an `attach`-mode session row
(`beta` above); it is the config's own `endpoint`, absent for `spawn`. This
document is built purely from local state (this body's session config), same
as always — it never reflects a live attach's real success/failure; `roster`
(§7) is the authoritative "is this session actually usable right now" view.

**Hub:**

```json
{
  "role": "hub", "protocol": 2, "protocol_min": 2, "protocol_max": 2,
  "hostname": "uranus", "listening": "ws://127.0.0.1:41807",
  "features": ["interrupt", "presence", "ping", "query", "roster", "token", "attach", "opencode-http"],
  "harnesses_known": ["opencode"],
  "harnesses_confirmed": [{"id": "opencode", "bodies": ["kiwi"]}],
  "bodies": 1, "sessions": 2
}
```

### 5.2 Caps document

`caps` is `status` **plus** an explicit map of every known feature /
harness → `{ok, kind, how?, reason?}`. A body's view lists the features
and harnesses **it** implements; the hub's view lists `harnesses_confirmed`
(a live body answered `query/support` `ok:true` — an advertisement alone
is not enough) and the protocol features the hub implements.

### 5.3 Support answer

`query/support {feature}` →

```json
{"feature": "opencode", "kind": "harness", "ok": true, "how": "opencode acp"}
```

or, when unsupported,

```json
{"feature": "claude", "kind": "harness", "ok": false, "reason": "no adapter"}
```

`kind` is `feature` | `harness` | `capability`. `ok` is decided by a
**local probe** (adapter + runnable harness), never by asking a model. A
`feature` id outside the vocabulary (§9) → error `-32006 unknown_feature`.

`attach` and `opencode-http` (`kind:"capability"`) are `ok:true`
unconditionally — both are implemented body-side, independent of any session
config. A **harness** id backed by an `attach`-mode session (issue #195) is
the one probe that is not purely local bookkeeping: it dials the configured
`endpoint` (`GET /session/{session_id}`, falling back to `/api/session/{id}`
— the same dual-prefix existence check `SessionManager` itself uses to
attach) and reports `ok:true` only while that endpoint actually answers —
`ok:false, reason:"attach endpoint did not answer"` the moment it stops.

### 5.4 Protocol answer

`query/protocol` (no `version`) reports the range — `{session:2, min,
max}`. With `{version: n}` it answers "can you speak `n`?" →
`{session:2, min, max, asked: n, ok}` where `ok = asked >= min && asked
<= max`. `n` must be a positive integer, else `-32006 unknown_feature`.

## 6. Talk

A **`session/prompt`** request (`{session, message, meta?}`) produces
**zero or more** `session/update` notifications and **then exactly one**
response when the turn ends — the **ACP shape, verbatim**:

```json
// request (hub → body)
{"jsonrpc":"2.0","id":"h-01H...","method":"session/prompt","params":{"session":"io/alpha","message":{... A2A Message, role:"user" ...}}}

// streamed chunks (body → hub), one per turn delta
{"jsonrpc":"2.0","method":"session/update","params":{"session":"io/alpha","prompt_id":"h-01H...","seq":1,"parts":[{"text":"Hel"}]}}
{"jsonrpc":"2.0","method":"session/update","params":{"session":"io/alpha","prompt_id":"h-01H...","seq":2,"parts":[{"text":"lo"}]}}

// exactly one response, when the turn ends
{"jsonrpc":"2.0","id":"h-01H...","result":{"stopReason":"end_turn","state":"completed","message":{... A2A Message, role:"agent" ...}}}
```

- `message` in the response is an A2A `Message` with `role:"agent"`; **`message.parts` is the full reply** — the **coalescer merges consecutive text parts** into one before it is sent, so the agent's reply is one logical text (interleaved `raw`/`url`/`data` parts are kept as their own parts).
- `seq` is a per-`prompt_id` monotonic counter; `prompt_id` is the **request's `id`**, so each `session/update` is correlated to the prompt that produced it.
- **`stopReason` is the source of truth** (the ACP value, carried verbatim). The **A2A terminal `state` is derived from it at the body**, so the A2A bridge is a pass-through:

  | ACP `stopReason` | → A2A terminal `state` |
  | --- | --- |
  | `end_turn` | `completed` |
  | `cancelled` | `canceled` |
  | `error` | `failed` |
  | `refusal` | `rejected` |
  | `max_tokens` | `failed` (with reason) |
  | `max_turn_requests` | `failed` (with reason) |
  | `limit` | `failed` |

- **Cancellation** is a **`session/cancel`** request → a response
  **`{applied:true}`** sent **only after** the cancel was actually applied
  (called ACP `session/cancel` / the OpenCode-HTTP fallback — not merely
  parsed). **That response is the acknowledgement** the interruption path
  wanted (v1's `ack`/`of`). The hub waits for it on a short, RTT-scaled
  timeout: `max(2s, 10 × last measured circuit/ping RTT)` (issue #191,
  `holler-hub`'s `interrupt::ack_timeout`) — distinct from, and deliberately
  blind to, the connection's own heartbeat/liveness cadence, since a
  round-trip budget and "has this body gone silent" are different questions.
  Timing out here is `interrupt sent but not confirmed within Ns (body may
  be stalled); the session is still on the roster` — the session is **not**
  assumed cancelled or dropped, only unconfirmed.
- **Priority path**: on both peers, a `session/cancel` (hub→body) and its
  `{applied:true}` response (body→hub) travel a **separate, high-priority
  outbound channel** from `session/prompt`/`session/update` traffic, so a
  cancel already queued for send is never stuck behind a large in-flight
  `session/update` flush or another prompt's own outbound frames.
- **`interrupt SESSION TEXT`** (the redirect form, issue #191) is one atomic
  operator action: `session/cancel`, await `{applied:true}` (RTT-scaled,
  above), then a fresh `session/prompt` — this second request sets
  `replace:true` in its params (`{session, message, replace:true}`) so the
  body dispatches it as a `SessionCommand::Replace` (cancel-then-run-ahead-
  of-the-queue) rather than the plain busy-check path — and its reply
  streams exactly like a normal `say`. `replace` is absent (and defaulted to
  `false` on decode) on every pre-#191 `Prompt`, so existing fixtures are
  byte-identical.
- **Reconnect** has no durable replay: the only signal is the affected
  session's `state` returning to `idle`; the peer is expected to re-query
  ("ask again"), not rely on durable delivery.

## 7. Presence

`session/presence` is a **notification** (body→hub), sent periodically as
a heartbeat and on every (re)connect:

```json
{"jsonrpc":"2.0","method":"session/presence","params":{"hostname":"kiwi","sessions":[
  {"name":"alpha","harness":"opencode","state":"idle","mode":"spawn"},
  {"name":"beta","harness":"opencode","state":"working","mode":"spawn","harness_session_id":"openc-abc",
   "turn_started_at":"2026-09-08T12:00:00Z","last_update_at":"2026-09-08T12:00:03Z",
   "turn_id":"h-01H...",
   "last_turn":{"turn_id":"h-01H...prev","state":"completed","stop_reason":"end_turn","ended_at":"2026-09-08T11:59:50Z"}},
  {"name":"gamma","harness":"opencode","state":"input-required","mode":"spawn",
   "pending":[{"id":"perm-1","kind":"permission","prompt":"Run `rm -rf /`?","options":["allow","reject"]},
               {"id":"el-1","kind":"elicitation","prompt":"Which branch to merge?","options":["main","release/1.0"]}],
   "turn_id":"h-01H..."}
]}}
```

- `state` is an **A2A session state**: `idle` | `working` | `input-required` (ADR 0005 adds `idle` — A2A states are per *task*, Holler presence is per *session*).
- `mode` is `spawn` | `attach`. `harness_session_id` is present for `attach` (the existing harness session being attached to).
- `turn_started_at` / `last_update_at` (RFC 3339) are present **only** while `state` is `working` (issue #150): they back the `-32009 session_busy` refusal's `turn_age_ms`/`last_update_age_ms` and the roster's derived `stalled` display (`working` with no update for `HOLLER_STALL_MS`, default 120 s — not yet computed anywhere; the roster/session-dispatch story that reads these fields at runtime is separate from this one).
- `pending` is present **only** while `state` is `input-required` (issue #151): one `{"id","kind","prompt","options"}` item per resolvable field, where `kind` is `permission` (ACP `session/request_permission`) or `elicitation` (ACP `elicitation/create`) and `prompt` is the permission's own `title` or the elicitation's own `message`. The roster's `PENDING` column (the roster story #340) renders from these, and `answer SESSION CHOICE` (§4/§10.1) resolves the block they belong to (`id` is a stable per-field handle within the held block, not itself part of `answer`'s own `choice` grammar). `AcpDriver::pending` (`crates/holler-body/src/acp_driver.rs`) builds this list from the live held request and `session_manager::task::set_state` copies it onto presence on every `working`↔`input-required` transition — this is a runtime-consumed field, not a declared-only one.
- `turn_id` (issue #142) is the JSON-RPC `id` of the current or most recent `session/prompt` — **absent before the first prompt** a session has ever received, and independent of `state` once present. `holler wait SESSION --after TURN_ID` compares this against its caller-held watermark so it re-matches only a *new* terminal turn, never the one already reported.
- `last_turn` (issue #142) is `{turn_id, state, stop_reason, ended_at}`, present once a turn has ended and updated on every subsequent turn; it survives a reconnect (carried in presence, not just the one-shot `session/prompt` response). `state` is the same A2A terminal vocabulary as the `session/prompt` result (§6 table): `completed` | `canceled` | `failed` | `rejected`. `stop_reason` is the ACP value verbatim; `ended_at` is RFC 3339, matching the `turn_started_at`/`last_update_at` convention. The session-dispatch/live-connection machinery this depends on is **built**, not hypothetical: the hub's live-circuit loop (`circuit.rs`), its registry (`live.rs`), and the roster (`roster.rs`) are real and running; `circuit/join` is fully implemented, not a stub (`join.rs`, dispatched from `serve.rs`); and the body's own session machinery (`session_manager.rs`, `connection.rs`, `prompt_dispatch.rs`) runtime-consumes `turn_id`/`last_turn`/`turn_started_at`/`last_update_at` rather than merely declaring them. The roster's per-row wake primitive (`Roster::subscribe`, a `watch` channel bumped on every roster change) is what the `wait` CLI verb (below, `control/wait`) selects on — edge-triggered, never a polling loop. `wait` is **built**: it parses `--after`/`--until`/`--prefix`, calls `Roster::subscribe`, and replies `{matched, rows}`; see [`docs/orchestrating.md`](../orchestrating.md) for the pattern.
- The hub's **roster row is keyed by routable name** (`<label>/<session>`, ADR 0005); `client_id`, peer address, and connection handle are locator fields on that row and may change across a reconnect **without changing the name**.
- A claim for a name currently held by a **different, `connected` token** is **rejected** (`-32003 unknown_session`, `data.reason: "name held by another body"`); the **same token** re-advertising its own name always succeeds (the reconnect case). **Revised policy (issue #151, fixing holler-server#383):** a holder that has aged into `reconnecting` or `gone` no longer blocks a *different* token's claim — the newcomer **replaces** the row (the name is kept, its locators swap to the new token) rather than being permanently rejected once the original body drops without a graceful close. Because presence is **periodic**, a rejected claim (still possible against a live `connected` holder) is retried on the next beat — a lost presence race (holler-server#243) **cannot recur** — and once the stale holder ages past `reconnecting`/`gone`, the very next beat from the fresh claimant succeeds without a hub restart (the #383 defect this replaces).
- The hub **drops stale rows** (presence TTL) into the roster.

### Attach-mode sessions (issue #195)

`mode:"attach"` is dispatched to `HttpAttachDriver` instead of `AcpDriver`
(§`crates/holler-body/src/http_attach_driver.rs`) — it never spawns anything;
it attaches to a harness session another process already owns.

- **Eager, not lazy.** Unlike a `spawn`-mode session (whose driver comes up
  lazily, on its first `session/prompt`), an attach-mode session's task tries
  `HttpAttachDriver::attach` immediately when the body starts, before it ever
  accepts a command.
- **A failed attach never stops anything else.** Every session is its own
  independent task with no shared mutable state (this file's own §7 roster
  section, and `session_manager.rs`'s own module doc); a failed attach logs a
  `session attach_failed name=… reason=…` warning and retries every 30s from
  inside that same session's task — it can never stop a sibling session's
  task or the body's run as a whole.
- **Omitted from presence until it attaches.** A session whose initial attach
  has not yet succeeded is left out of `session/presence` entirely (not
  advertised as a fabricated `idle` row for a harness this body cannot
  actually reach) — it appears the moment a later retry succeeds.
- **Shutdown/detach/SIGINT never touch the attached process.** `HttpAttachDriver::
  shutdown` only ends this body's own background loop (the SSE connection,
  the question/permission poll) — no HTTP `DELETE`, no process kill (there is
  none to kill: this driver never spawned one). The same
  `SessionManager::shutdown` path a `spawn`-mode session's graceful teardown
  uses reaches an attach-mode session's driver too, and resolves to exactly
  this no-op-on-the-remote-side behavior for it.
- **The single-instance lock is unchanged.** Two `body run`s attaching the
  same `session_id` is still undefined, and the second is still refused by
  the existing `run.lock` exactly as before — attach mode adds no new
  locking of its own around the harness session itself.

### Reconnect contract

Issue #192 pins the behavioural contract at the moment of a drop, on both
sides — this is the section the earlier v0.1.0 milestone tested only half
of (rule 1's reply-tail-survives-a-reconnect case; never rule 4's
say-while-reconnecting case).

1. **Every (re)connect sends a full `circuit/hello` + `session/presence`**
   reflecting the body's *current* sessions and states, and the hub
   **replaces** that token's rows from the frame — it never merges. A row the
   token previously held but which the fresh frame omits goes `gone`
   immediately (`Roster::advertise`'s `drop_absent`, issue #186's own
   mechanism — this story only proves it end to end across a real drop and
   restart, it does not change it).
2. **Ask-again, never a durable replay.** The body keeps **no** buffer of a
   reply that completed while disconnected (declined in the v0.1.0 memo;
   recorded in ADR 0002). On reconnect the hub sees `state:idle` on presence
   for that session, and the operator re-`say`s — there is nothing to
   "catch up on".
3. **An in-flight prompt when the body's socket drops**: the hub fails the
   pending `say` with `-32008 connection_lost` **immediately** — on the
   drop itself, never after a timeout (`SessionConnection::fail_pending`,
   raced against every abrupt teardown path in `circuit.rs`). The body, on
   reconnect, does **not** resend updates for a prompt whose socket is
   gone: the dispatch task that owns that turn still holds its *old*
   connection's outbound channel, whose receiving half is dropped the
   moment that connection's task ends, so every further `session/update`
   (and the final `session/prompt` response) for that turn is silently
   discarded by construction — the turn still runs to completion locally,
   and the session returns to `idle` once it does.
4. **`say` while the body is `reconnecting`**: the hub answers
   `-32004 not_connected` immediately, naming the roster row's own
   `conn_state` and how long ago it was last seen in the message —
   `"io/alpha is reconnecting (last seen 32s ago)"` — rather than a bare
   "not connected" that hides whether the body might still come back.
   **No queuing at the hub in v1**: this is stated explicitly here so it is
   never a surprise. `reconnecting` itself is reached the same way an
   abrupt drop reaches it everywhere else in this section: a decode/send
   failure, a socket error, EOF, or the connection-level liveness timeout
   (issue #243) all mark the token's rows `reconnecting` immediately
   (`Roster::mark_reconnecting`) — never `gone` — so the body gets the same
   TTL grace window (`gone_secs`, default 180s) the roster's own sweep
   already gives a body that merely stops heartbeating. Only an *explicit*
   close (a clean WS close frame — `body detach`'s own teardown) still goes
   straight to `gone` (`Roster::clear`, holler-server#80): the body told the
   hub on its way out that it is not coming back.
5. **The body's prompt-side queue survives a drop.** A prompt already
   queued in the session's own mailbox before the drop still runs to
   completion (the session manager's tasks are independent of the WS
   connection, which is retried underneath them) — its reply is discarded
   per rule 3, exactly like the turn that was already running.
6. **A mid-turn blip that reconnects before the turn ends**: any
   `session/update` that still reaches the hub under the turn's original
   `prompt_id` — the hub, which already failed that `say` with
   `connection_lost` on the drop — ignores it and logs `debug orphan_update`
   rather than delivering it. Exactly-once for the operator: the failed
   `say` is the only outcome that ever reaches them; a fresh `say` starts a
   new turn under a new `prompt_id`.

## 8. Errors

JSON-RPC **error objects**. Standard codes for framing; **application
codes `-32000..-32099`** carry `data.code` = the Holler string.

| JSON-RPC code | `data.code` | When |
| --- | --- | --- |
| `-32700` | `parse_error` | The frame is not JSON. |
| `-32600` | `invalid_request` | Not a single JSON-RPC 2.0 object — a **batch** (top-level array), a **binary frame**, or a missing/`≠"2.0"` `jsonrpc` field. |
| `-32601` | `method_not_found` | Unknown `method`. |
| `-32602` | `invalid_params` | Schema violation in `params`. |
| `-32000` | `unsupported_version` | `hello.protocol ≠ 2`. Socket closes; **no silent downgrade**. |
| `-32001` | `join_failed` | `circuit/join` secret unknown / already bound / invalidated / revoked / expired — reason in `message`. |
| `-32002` | `unauthenticated` | Bad or revoked `credential`; **any** method sent before authenticate. |
| `-32003` | `unknown_session` | `prompt`/`cancel` to a name not hosted here, **or** a name held by a different token. |
| `-32004` | `not_connected` | A remote `query`/`ping` for a bound token with no live socket. |
| `-32005` | `session_superseded` | A new authenticate for the same token replaced this connection (also sent as the `circuit/superseded` notification; hub closes the old socket). |
| `-32006` | `unknown_feature` | A `query/support` (or `query/protocol` version) id outside the vocabulary. |
| `-32007` | `limit_exceeded` | A hub cap was hit (reserved; used by the caps story). |
| `-32008` | `connection_lost` | The body's socket dropped mid-turn (hub → CLI). |
| `-32009` | `session_busy` | `say` to a `working`/`stalled` session without `--queue` (issue #150), **or** to an `input-required` session (issue #151) — `--queue` never overrides the latter; `data.state:"input-required"` distinguishes it from the `working`/`stalled` case, and the CLI hint points at `answer`, not `interrupt`/`--queue`. |
| `-32010` | `nothing_pending` | `answer SESSION CHOICE` to a session that is not `input-required` — nothing to resolve (issue #151). |

`data` is an object `{"code": "<data.code>", "reason"?}`; `session_busy`
additionally carries `state` (the session's A2A state at refusal time),
`turn_age_ms`, and `last_update_age_ms`, so the caller's next command is
the hint, not a guess. `nothing_pending` carries only `code` (plus `reason`
when the refusal is contextual) — the session's `pending` list is already
advertisable in presence, so the refusal needs no extra payload. **`-32603` (internal error) is reserved** and
unused in v2. An **unmatched response** (a response whose `id` matches
no outstanding request) is an application condition: the peer logs and
discards it; it has no JSON-RPC code in v2.

## 9. Feature and harness ids

**Protocol features (v2):**

`interrupt`, `presence`, `ping`, `query`, `roster`, `token`, `attach`, `opencode-http`

**Harness ids** (vocabulary; only `opencode` is a v2 adapter):

`opencode`, `claude`, `codex`, `grok`, `hermes`, `pi`, `cursor`, `copilot`, `droid`, `kimi`, `qwen`, `kilo`, `goose`

(`wait` was dropped in v1 and is not in v2. A new harness is a new **config row** plus a new id — not a protocol bump.) Unknown ids are legal on the wire; `query/support` on an unknown id returns `ok:false` / `-32006` rather than inventing an answer.

## 10. CLI mapping

The complete CLI surface is **normative in [ADR 0003](../adr/ADR-0003.md)**
— this section reproduces its table verbatim so the spec never diverges from
the implementation. Every line in the block below is parsed against the real
clap tree by `crates/holler-cli/tests/docs_cli_test.rs`; the full flag
surface (every verb and flag) is `crates/holler-cli/tests/fixtures/cli-surface.txt`.
A local `query` (no `TARGET`) runs against this process; with a `TARGET` the
hub sends the same command to that body over the wire. `SESSION`/`TARGET`
expand under ADR 0005 (`<label>/<session>`; a bare `<session>` when exactly
one roster row matches).

```text
holler --version | -V                       prints `holler X.Y.Z` (workspace version)
holler --help
global flags on every subcommand:  --debug none|quiet|noisy   --log-format text|json   --json

holler hub serve [--listen HOST:PORT]... [--advertise HOST[:PORT]]
holler hub token mint --label LABEL [--ttl 24h] [--json]
holler hub token list [--json]
holler hub token delete ID            (aliases: rm, remove) — unused token: invalidate the secret
holler hub token revoke ID            (alias of delete for a BOUND token) — cut the body's access now, keep the record
holler hub token ping ID [--json]
holler hub status [--json]
holler hub caps [--json]
holler hub support FEATURE [--json]
holler hub query CMD [ARGS...] [--json]               (local: this hub process)
holler hub query TARGET CMD [ARGS...] [--json]        (remote: TARGET = token id | client id | label | label/session)
holler roster [--json]
holler say SESSION TEXT [--timeout 600s] [--queue] [--json]     refused (-32009) while the turn is busy unless --queue
holler interrupt SESSION [TEXT]                                  cancel; with TEXT, then prompt (one atomic step)
holler answer SESSION CHOICE                                     resolve a held input-required (permission / elicitation)
holler wait SESSION[,SESSION...] [--until STATES] [--after TURN_ID] [--timeout 600s] [--json]

holler body join --server URL --token ID:SECRET
holler body run [--config PATH]
holler body detach
holler body status [--json]
holler body caps [--json]
holler body support FEATURE [--json]
holler body query CMD [ARGS...] [--json]
holler body attach sessions [--endpoint URL]
holler body attach init [--endpoint URL] [--session ID] [--name NAME] [--out PATH] [--force]
```

`--json` is a **global** flag: it prints the result body as exactly one JSON
object on stdout, all diagnostics on stderr. Feature ids are reserved words.

### 10.1 Which wire method each verb sends

`query/*` and `session/*` are the only verbs that carry a wire payload (the
rest are local state or control). Local (no target) runs in-process; remote
routes to the named body.

| CLI | Wire |
| --- | --- |
| `hub status` / `body status` | local `query/status` (no target) or `query/status` to that body |
| `hub caps` / `body caps` / `hub query <target> caps` | `query/caps` |
| `hub support FEATURE` / `body support FEATURE` | local `query/support` / to that body |
| `hub query protocol [N]` / `hub query <target> protocol [N]` | `query/protocol` (highest version) / `{version:N}` (can you speak N?) |
| `say SESSION TEXT` | `session/prompt` to that body (ADR 0005 addressing) |
| `interrupt SESSION [TEXT]` | `session/cancel`, then `session/prompt` when `TEXT` is given |
| `answer SESSION CHOICE` | `session/answer` to that body (§4), forwarded by the hub over the same per-connection generic-request path `hub query <target> …`'s row above already uses (`control/answer` → `LiveHandle::query("session/answer", …)`) |
| `wait SESSION[,…]` | no wire call: edge-triggered on the hub's roster |
| `roster` | local roster (presence TTL) |
| `hub token ping ID` | `circuit/ping` (bound-socket aliveness; not a query) |

## 11. Not in v2

- **Batches** (a JSON-RPC array per frame) — deferred to a version bump.
- **Binary frames** — rejected (`-32600`); all content is text (a file's bytes are a base64 `raw` Part).
- **Multi-hop routing** — the circuit is 1:1 hub↔body.
- **Model-answered queries** — `query/*` are control, never routed into a model.
- **A2A reachability features** — the HTTP client→server transport, one reachable endpoint per agent, push-notification webhooks, and agent cards. Those assume a reachability the body does not have; they live **only at the hub's outward face** (ADR 0017).
- **A `v0` dialect**, SSH/Tailscale as the app protocol, secrets in logged URLs, PTY multiplexing, and durable replay of a reply that completed while the body was disconnected.
